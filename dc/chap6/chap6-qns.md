# Chapter 6: Impairments, Error handling and Compression Techniques - Past Questions

### 2024 Spring
```
4. (a) Briefly explain attenuation, distortion and noise that occurs in data communication. Why are they called transmission impairments? [7]
4. (b) If the frame is x^7 + x^5 + 1 by the generator polynomial x^3 + 1, what would be the transmitted frame using CRC? [8]
OR
Encode the given five source symbols (messages) using Huffman codes and find its efficiency: [8]
Message: m1, m2, m3, m4, m5
Probability: 0.4, 0.2, 0.2, 0.1, 0.1
```

### 2023 Fall (2024 Spring Back)
```
4. (a) Why burst error is difficult to handle than single error? Explain why CRC is better than parity method of error detection. [8]
OR
Explain various transmission impairments of a communication system. [8]
4. (b) Compare and contrast between lossless and lossy compression. [7]
```

### 2023 Spring (2023 A Front / Back)
```
6. b) What is transmission impairment? Describe various transmission impairments that occur in a communication system. [8]
8. a) Why is the CRC method better than checksum for error detection? If the frame is 1101011011 and generator is 10011, what would be the transmitted frame using the CRC method? [8]
8. b) Compare and contrast between lossless and lossy data compression. Encode the given source using the Huffman coding method and find its efficiency: [7]
m1: 0.25, m2: 0.25, m3: 0.2, m4: 0.15, m5: 0.15
```

### 2022 B Back (2019 B Content)
```
3. a) Why burst error is difficult to handle than single bit error? Explain properly. [8]
3. b) Why is the CRC better than the checksum for error detection? If the frame is 1101011011 and generator is 10011, what would be the transmitted frame using CRC? [7]
```
*(Note: 2022 B Back content 2019 B)*

### 2021 A Back
```
7. (a) Why burst error is difficult to handle than single error? Describe why CRC is better than checksum for error detection. [8]
7. (b) Compare and contrast between lossless and lossy data compression. Encode the following messages using Huffman coding and find efficiency. [7]
m1 = 0.4, m2 = 0.2, m3 = 0.2, m4 = 0.1, m5 = 0.1
```

### 2018 B Back
```
6. (a) Why burst error is difficult to handle than single error? If the frame is 1001110110 and generator is 1011, what would be the transmitted frame using CRC method? [8]
7. (a) What is transmission impairment? Briefly describe various transmission impairments that occur in a communication system. [8]
7. (b) Distinguish between lossy and lossless data compression. Encode the given messages using Huffman codes and find efficiency. [7]
m1 = 0.2, m2 = 0.2, m3 = 0.2, m4 = 0.2, m5 = 0.2
```

### 2017 (2021 B Back)
```
6. (a) Why burst error is difficult to handle than single error? If the frame is 1101011011 and generator is 10011, what would be the transmitted frame using CRC? [8]
7. (a) What is transmission impairment? Briefly describe various transmission impairments. [8]
7. (b) Differentiate between lossless and lossy compression. Encode following symbols using Huffman coding. [7]
m1 = 0.3, m2 = 0.25, m3 = 0.25, m4 = 0.1, m5 = 0.1
```
