# Chapter 3: Numerical Differentiation and Integration - Past Questions
### Quick Navigation
[3(a)](#3a) | [3(b)](#3b)

---

## 3(a)

##### 2025 Spring - Pokhara University
a) Compute the Simpson's 1/3 and Simpson's 3/8 rule for $I = \int_0^1 e^{-x^2} \, dx$ using a regular partition with subinterval $n=6$. **[8]**

```markdown
a) Compute the Simpson's 1/3 and Simpson's 3/8 rule for $I = \int_0^1 e^{-x^2} \, dx$ using a regular partition with subinterval $n=6$. **[8]**
```

---

##### 2024 Spring - Pokhara University
a) Find $I = \int_{0.2}^{1.4} \left( \sin x - \ln x + e^x \right) \, dx$ by using: **[8]**
   i. Trapezoidal rule
   ii. Simpson's 1/3 rule
   iii. Simpson's 3/8 rule

```markdown
a) Find $I = \int_{0.2}^{1.4} \left( \sin x - \ln x + e^x \right) \, dx$ by using: **[8]**
   i. Trapezoidal rule
   ii. Simpson's 1/3 rule
   iii. Simpson's 3/8 rule
```

---

##### 2023 Spring - Pokhara University
a) Use Romberg's method to compute:
   $$I = \int_0^2 \frac{e^x + \sin x}{2} \, dx$$
   correct up to two decimal places. **[8]**

```markdown
a) Use Romberg's method to compute:
   $$I = \int_0^2 \frac{e^x + \sin x}{2} \, dx$$
   correct up to two decimal places. **[8]**
```

---

## 3(b)

##### 2025 Spring - Pokhara University
b) Use the Romberg integration to find the solution of $I = \int_0^1 \frac{1}{1 + x^2} \, dx$ correct up to three decimal places. **[7]**

```markdown
b) Use the Romberg integration to find the solution of $I = \int_0^1 \frac{1}{1 + x^2} \, dx$ correct up to three decimal places. **[7]**
```

---

##### 2024 Spring - Pokhara University
b) Use the Romberg integration to find the solution of $I = \int_0^1 \frac{dx}{1 + x^2}$ correct up to three decimal places. **[7]**

```markdown
b) Use the Romberg integration to find the solution of $I = \int_0^1 \frac{dx}{1 + x^2}$ correct up to three decimal places. **[7]**
```

---

##### 2023 Spring - Pokhara University
b) Estimate the approximate derivative of $f(x) = x^2$ at $x=1$ for $h = 0.1, 0.2, 0.05, 0.01$. Use the first-order difference method and find the respective errors. **[7]**

```markdown
b) Estimate the approximate derivative of $f(x) = x^2$ at $x=1$ for $h = 0.1, 0.2, 0.05, 0.01$. Use the first-order difference method and find the respective errors. **[7]**
```