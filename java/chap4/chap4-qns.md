# Chapter 4: Distributed Network Programming - Past Questions
### Quick Navigation
[4(b)](#4b) | [5(a)](#5a) | [5(b)](#5b)

---

## 4(b)

##### 2024 Fall - Pokhara University
b) What is socket in Java? Write a TCP client-server program where the server sends an integer and the client responds by sending the square of the integer. **[7]**

```markdown
b) What is socket in Java? Write a TCP client-server program where the server sends an integer and the client responds by sending the square of the integer. **[7]**
```

---

## 5(a)

##### 2020 Fall - Pokhara University
a) What are the different socket types used in Java? How do they differ? **[8]**

```markdown
a) What are the different socket types used in Java? How do they differ? **[8]**
```

---

##### 2022 Fall - Pokhara University
a) Create a TCP client-server program in which the client sends a message and the server receives the message from the client and replies back. **[8]**

```markdown
a) Create a TCP client-server program in which the client sends a message and the server receives the message from the client and replies back. **[8]**
```

---

##### 2023 Fall - Pokhara University
a) Write a program to send "Message from Pokhara University" from a client to a server using UDP. **[8]**

```markdown
a) Write a program to send "Message from Pokhara University" from a client to a server using UDP. **[8]**
```

---

##### 2024 Spring - Pokhara University
a) Differentiate between UDP and TCP. Create a UDP server that echoes the message sent by a sender. **[8]**

```markdown
a) Differentiate between UDP and TCP. Create a UDP server that echoes the message sent by a sender. **[8]**
```

---

##### 2025 Spring - Pokhara University
a) Create a TCP client-server application where a client sends a string and the server responds by sending the length of the string. **[8]**

```markdown
a) Create a TCP client-server application where a client sends a string and the server responds by sending the length of the string. **[8]**
```

---

## 5(b)

##### 2022 Fall - Pokhara University
b) Write a program to send "Message from Pokhara University" from a client to a server using UDP. **[7]**

```markdown
b) Write a program to send "Message from Pokhara University" from a client to a server using UDP. **[7]**
```

---

##### 2023 Fall - Pokhara University
b) Define URL and its components. Write a program to display the different components of a URL. **[7]**

```markdown
b) Define URL and its components. Write a program to display the different components of a URL. **[7]**
```

---

##### 2024 Spring - Pokhara University
b) Define URL and URL Connection class. Write a program to illustrate URL Connection class in Java. **[7]**

```markdown
b) Define URL and URL Connection class. Write a program to illustrate URL Connection class in Java. **[7]**
```

---

##### 2025 Spring - Pokhara University
b) Explain about multi-threading. Write a program to show the example of creating a multithreaded Java server.  
**OR**  
Create a multithreaded TCP server that can take messages from multiple users and reply back to them individually. Terminate communication on "Stop" word and start communication on "Start" word. **[7]**

```markdown
b) Explain about multi-threading. Write a program to show the example of creating a multithreaded Java server.  
**OR**  
Create a multithreaded TCP server that can take messages from multiple users and reply back to them individually. Terminate communication on "Stop" word and start communication on "Start" word. **[7]**
```\n