# Chapter 4: Fourier Integral and Transform — Past Questions
### Quick Navigation
[4(a) — Fourier Integral Show](#4a--fourier-integral-show) | [4(b) — Fourier Cosine/Sine Transform](#4b--fourier-cosinesine-transform) | [5(a) — Fourier Integral](#5a--fourier-integral) | [5(b) — Fourier Cosine Transform + Parseval](#5b--fourier-cosine-transform--parseval) | [6(a) — Fourier Cosine Transform / Convolution](#6a--fourier-cosine-transform--convolution) | [6(b) — Fourier Integral / Parseval / FIT](#6b--fourier-integral--parseval--fit) | [Short Questions](#short-questions)

---

> [!NOTE]
> 🏛️ = **Pokhara University Board Exam** &nbsp;|&nbsp; All other entries are internal assessments, pre-board, or affiliate college exams.

---

## 4(a) — Fourier Integral Show

##### 🏛️ 2025 Spring — Pokhara University (Board)
a) Using Fourier integral representation, show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0 & \text{if } x < 0 \\ \frac{\pi}{2} & \text{if } x = 0 \\ \pi e^{-x} & \text{if } x > 0 \end{cases}$$
**[7]**

```markdown
a) Using Fourier integral representation, show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0 & \text{if } x < 0 \\ \frac{\pi}{2} & \text{if } x = 0 \\ \pi e^{-x} & \text{if } x > 0 \end{cases}$$
**[7]**
```

---

##### 2024 Spring — NAST (Pre-University)
a) Show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[7]**

```markdown
a) Show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[7]**
```

---

##### 2024 Spring — NCIT (Assessment)
a) Show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[7]**

```markdown
a) Show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[7]**
```

---

##### 2024 Spring — Nepal Engineering College / NEC (Assessment)
a) Show that:
$$\int_0^\infty \frac{\sin w \cos xw}{w} \, dw = \begin{cases} \frac{\pi}{2}, & 0 \le x < 1 \\ \frac{\pi}{4}, & x = 1 \\ 0, & x > 1 \end{cases}$$
**[8]**

```markdown
a) Show that:
$$\int_0^\infty \frac{\sin w \cos xw}{w} \, dw = \begin{cases} \frac{\pi}{2}, & 0 \le x < 1 \\ \frac{\pi}{4}, & x = 1 \\ 0, & x > 1 \end{cases}$$
**[8]**
```

---

##### 2024 Spring — Madan Bhandari College of Engineering / MBCE (Final Internal)
a) Define Fourier integral. By choosing a suitable function, show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[8]**

```markdown
a) Define Fourier integral. By choosing a suitable function, show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[8]**
```

---

##### 2024 Spring — Pokhara Engineering College / PEC (Final Internal)
a) Show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[8]**

```markdown
a) Show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[8]**
```

---

##### 2024 Spring — Universal Engineering & Science College / UESC (Pre-Board)
a) Find the Fourier integral representation of the function:
$$f(x) = \begin{cases} 1, & \text{if } 0 < x < 1 \\ \frac{1}{2}, & \text{if } x = 1 \\ 0, & \text{if } x > 1 \end{cases}$$
and hence evaluate:
$$\int_0^\infty \frac{\sin w \cos xw}{w} \, dw$$
**[7]**

```markdown
a) Find the Fourier integral representation of the function:
$$f(x) = \begin{cases} 1, & \text{if } 0 < x < 1 \\ \frac{1}{2}, & \text{if } x = 1 \\ 0, & \text{if } x > 1 \end{cases}$$
and hence evaluate:
$$\int_0^\infty \frac{\sin w \cos xw}{w} \, dw$$
**[7]**
```

---

##### 2024 Spring — Everest Engineering College (Pre-Board)
a) Find the Fourier integral representation of the function:
$$f(x) = \begin{cases} e^{-x}, & x > 0 \\ 0, & x < 0 \end{cases}$$
and show that at $x = 0$ it converges to $\frac{1}{2}$. **[7]**

```markdown
a) Find the Fourier integral representation of the function:
$$f(x) = \begin{cases} e^{-x}, & x > 0 \\ 0, & x < 0 \end{cases}$$
and show that at $x = 0$ it converges to $\frac{1}{2}$. **[7]**
```

---

##### 2024 Fall — United Technical College / UTC (Assessment)
a) State and prove the Convolution Theorem for Fourier transform. **[8]**

```markdown
a) State and prove the Convolution Theorem for Fourier transform. **[8]**
```

---

## 4(b) — Fourier Cosine/Sine Transform

##### 🏛️ 2025 Fall — Pokhara University (Board)
a) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$. **[7]**

```markdown
a) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$. **[7]**
```

---

##### 🏛️ 2025 Spring — Pokhara University (Board)
b) Find the Fourier cosine transform of $f(x) = e^{-x}$ and by using Parseval's identity, show that:
$$\int_0^\infty \frac{dx}{(1+x^2)^2} = \frac{\pi}{4}$$
**[8]**

```markdown
b) Find the Fourier cosine transform of $f(x) = e^{-x}$ and by using Parseval's identity, show that:
$$\int_0^\infty \frac{dx}{(1+x^2)^2} = \frac{\pi}{4}$$
**[8]**
```

---

##### 🏛️ 2024 Fall — Pokhara University (Board)
b) Show that:
$$\int_0^\infty \frac{\cos wx + w \sin wx}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[8]**

```markdown
b) Show that:
$$\int_0^\infty \frac{\cos wx + w \sin wx}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[8]**
```

---

##### 2024 Spring — NAST (Pre-University)
b) Find the Fourier sine transform of the function:
$$f(x) = \begin{cases} x^2, & \text{for } 0 < x < 1 \\ 0, & \text{for } x > 1 \end{cases}$$
**[8]**

**OR**

Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$ and then show that:
$$\int_0^\infty \frac{\cos kx}{m^2 + x^2} \, dx = \frac{\pi}{2m} e^{-mk}$$

```markdown
b) Find the Fourier sine transform of the function:
$$f(x) = \begin{cases} x^2, & \text{for } 0 < x < 1 \\ 0, & \text{for } x > 1 \end{cases}$$
**[8]**

**OR**

Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$ and then show that:
$$\int_0^\infty \frac{\cos kx}{m^2 + x^2} \, dx = \frac{\pi}{2m} e^{-mk}$$
```

---

##### 2024 Spring — NCIT (Assessment)
b) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$ and then show that:
$$\int_0^\infty \frac{\cos kx}{m^2 + x^2} \, dx = \frac{\pi}{2m} e^{-mk}$$
**[8]**

```markdown
b) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$ and then show that:
$$\int_0^\infty \frac{\cos kx}{m^2 + x^2} \, dx = \frac{\pi}{2m} e^{-mk}$$
**[8]**
```

---

##### 2024 Spring — Nepal Engineering College / NEC (Assessment)
b) Find the Fourier transform of $e^{-|x|}$, and hence evaluate the integral:
$$\int_0^\infty \frac{x \sin mx}{1 + x^2} \, dx$$
**[7]**

```markdown
b) Find the Fourier transform of $e^{-|x|}$, and hence evaluate the integral:
$$\int_0^\infty \frac{x \sin mx}{1 + x^2} \, dx$$
**[7]**
```

---

##### 2024 Spring — Madan Bhandari College of Engineering / MBCE (Final Internal)
b) Find the Fourier transform of the function $f(x) = e^{-|x|}$. **[7]**

```markdown
b) Find the Fourier transform of the function $f(x) = e^{-|x|}$. **[7]**
```

---

##### 2024 Spring — Universal Engineering & Science College / UESC (Pre-Board)
b) Define the convolution of two functions. State and prove the convolution theorem on Fourier transform. **[8]**

```markdown
b) Define the convolution of two functions. State and prove the convolution theorem on Fourier transform. **[8]**
```

---

##### 2024 Spring — Lumbini Engineering, Management & Science College / LEMSC (Final Internal)
a) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$. **[8]**

b) What are the Fourier sine and cosine integrals of a function $f(x)$? Represent:
$$f(x) = \begin{cases} \cos x, & |x| < \frac{\pi}{2} \\ 0, & |x| > \frac{\pi}{2} \end{cases}$$
as a Fourier integral, and hence show that:
$$\int_0^\infty \frac{\cos\left(\frac{w\pi}{2}\right) \cos xw}{1 - w^2} \, dw = \begin{cases} \frac{\pi}{2} \cos x, & |x| < \frac{\pi}{2} \\ 0, & |x| > \frac{\pi}{2} \end{cases}$$
**[7]**

```markdown
a) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$. **[8]**

b) What are the Fourier sine and cosine integrals of a function $f(x)$? Represent:
$$f(x) = \begin{cases} \cos x, & |x| < \frac{\pi}{2} \\ 0, & |x| > \frac{\pi}{2} \end{cases}$$
as a Fourier integral, and hence show that:
$$\int_0^\infty \frac{\cos\left(\frac{w\pi}{2}\right) \cos xw}{1 - w^2} \, dw = \begin{cases} \frac{\pi}{2} \cos x, & |x| < \frac{\pi}{2} \\ 0, & |x| > \frac{\pi}{2} \end{cases}$$
**[7]**
```

---

##### 2024 Fall — United Technical College / UTC (Assessment)
b) Find the Fourier integral representation of the function:
$$f(x) = \begin{cases} e^{-ax}, & x > 0 \\ e^{ax}, & x < 0 \end{cases}$$
where $a > 0$. **[7]**

```markdown
b) Find the Fourier integral representation of the function:
$$f(x) = \begin{cases} e^{-ax}, & x > 0 \\ e^{ax}, & x < 0 \end{cases}$$
where $a > 0$. **[7]**
```

---

## 5(a) — Fourier Integral

##### 2024 Spring — Pokhara University (Board)
a) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$, and then show that:
$$\int_0^\infty \frac{\cos kx}{m^2 + x^2} \, dx = \frac{\pi}{2m} e^{-mk}$$
**[7]**

```markdown
a) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$, and then show that:
$$\int_0^\infty \frac{\cos kx}{m^2 + x^2} \, dx = \frac{\pi}{2m} e^{-mk}$$
**[7]**
```

---

##### 2024 Spring — Pokhara Engineering College / PEC (Final Internal)
b) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$ and then show that:
$$\int_0^\infty \frac{\cos kx}{m^2 + x^2} \, dx = \frac{\pi}{2m} e^{-mk}$$
**[7]**

```markdown
b) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$ and then show that:
$$\int_0^\infty \frac{\cos kx}{m^2 + x^2} \, dx = \frac{\pi}{2m} e^{-mk}$$
**[7]**
```

---

## 5(b) — Fourier Cosine Transform + Parseval

##### 🏛️ 2024 Spring — Pokhara University (Board)
b) Show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[8]**

```markdown
b) Show that:
$$\int_0^\infty \frac{\cos xw + w \sin xw}{1 + w^2} \, dw = \begin{cases} 0, & \text{if } x < 0 \\ \frac{\pi}{2}, & \text{if } x = 0 \\ \pi e^{-x}, & \text{if } x > 0 \end{cases}$$
**[8]**
```

---

##### 2024 Spring — Everest Engineering College (Pre-Board)
b) Find the Fourier sine transform of $e^{-x}$ and by using Parseval's Identity, show that:
$$\int_0^\infty \frac{x^2}{(1 + x^2)^2} \, dx = \frac{\pi}{4}$$
**[8]**

```markdown
b) Find the Fourier sine transform of $e^{-x}$ and by using Parseval's Identity, show that:
$$\int_0^\infty \frac{x^2}{(1 + x^2)^2} \, dx = \frac{\pi}{4}$$
**[8]**
```

---

## 6(a) — Fourier Cosine Transform / Convolution

##### 🏛️ 2024 Fall — Pokhara University (Board)
a) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$, and then show that:
$$\int_0^\infty \frac{\cos kx}{1 + x^2} \, dx = \frac{\pi}{2} e^{-k}$$
**[7]**

```markdown
a) Find the Fourier cosine transform of $f(x) = e^{-mx}$ for $m > 0$, and then show that:
$$\int_0^\infty \frac{\cos kx}{1 + x^2} \, dx = \frac{\pi}{2} e^{-k}$$
**[7]**
```

---

##### 2024 Spring — Lumbini Engineering, Management & Science College / LEMSC (Final Internal)
b) State and prove the Fourier Integral Theorem. **[7]**

```markdown
b) State and prove the Fourier Integral Theorem. **[7]**
```

---

## 6(b) — Fourier Integral / Parseval / FIT

##### 🏛️ 2025 Fall — Pokhara University (Board)
b) Show that:
$$\int_0^\infty \frac{w \sin xw}{a^2 + w^2} \, dw = \frac{\pi}{2} e^{-ax}$$
where $x > 0, a > 0$. **[5]**

```markdown
b) Show that:
$$\int_0^\infty \frac{w \sin xw}{a^2 + w^2} \, dw = \frac{\pi}{2} e^{-ax}$$
where $x > 0, a > 0$. **[5]**
```

---

##### 31 — Pokhara University
d) Write the Fourier cosine and sine integral representations for the function $f(x)$.

```markdown
d) Write the Fourier cosine and sine integral representations for the function $f(x)$.
```

---

## Short Questions

##### 🏛️ 2025 Spring — Pokhara University (Board) — Attempt all (4 × 2.5)
a) Evaluate:
$$\oint_C \frac{2z}{(z-1)(z+3)} \, dz$$
where $C$ is the circle $C: |z| = 2$ counter-clockwise, by using Cauchy residue theorem.

b) Check whether the function $u(x, y) = z\bar{z}$ is analytic or not.

c) Find $Z(n^2)$.

d) Solve $u_x + u_y = 0$ by using separation of variables.

```markdown
a) Evaluate $\oint_C \frac{2z}{(z-1)(z+3)} dz$, $C: |z|=2$, using Cauchy residue theorem.
b) Check whether $u(x,y) = z\bar{z}$ is analytic.
c) Find $Z(n^2)$.
d) Solve $u_x + u_y = 0$ by separation of variables.
```

---

##### 🏛️ 2024 Fall — Pokhara University (Board) — Attempt any two (2 × 5)
a) Find Laurent series of $f(z) = \frac{z+3}{z(z+1)(z-2)}$ in the region $1 < |z| < 2$.

b) Find the solution of the differential equation $y^2 u_x - x^2 u_y = 0$ by separating variables.

c) Show that $u(x, y) = e^{2x}(x \cos 2y - y \sin 2y)$ is a harmonic function. Find its harmonic conjugate.

```markdown
a) Find Laurent series of $f(z) = \frac{z+3}{z(z+1)(z-2)}$ in $1 < |z| < 2$.
b) Find the solution of $y^2 u_x - x^2 u_y = 0$ by separating variables.
c) Show $u(x,y) = e^{2x}(x\cos 2y - y\sin 2y)$ is harmonic. Find its harmonic conjugate.
```

---

##### 2024 Spring — NCIT (Assessment) — Attempt all (4 × 2.5)
a) Show that the Fourier sine transform is a linear operator.

```markdown
a) Show that the Fourier sine transform is a linear operator.
```

---

##### 2024 Spring — Lumbini Engineering, Management & Science College / LEMSC (Final Internal) — Solve (5 × 2)
b) Prove that $\int_0^\infty \frac{\sin x}{x} \, dx = \frac{\pi}{2}$ using Fourier transform.

```markdown
b) Prove that $\int_0^\infty \frac{\sin x}{x} \, dx = \frac{\pi}{2}$ using Fourier transform.
```

---

##### 2024 Spring — Madan Bhandari College of Engineering / MBCE (Final Internal) — Short
b) State the convolution theorem for Fourier transforms.

```markdown
b) State the convolution theorem for Fourier transforms.
```

---

##### 2024 Spring — NAST (Pre-University) — Solve (4 × 2.5)
c) Show that Fourier cosine transforms satisfy the linearity property.

```markdown
c) Show that Fourier cosine transforms satisfy the linearity property.
```

---

##### 2024 Spring — Everest Engineering College (Pre-Board) — Short (4 × 2.5)
d) Let $f(x)$ be continuous on $\mathbb{R}$, $f(t) \to 0$ as $t \to \infty$, and $f'(x)$ be absolutely integrable on $\mathbb{R}$. Show that:
$$\mathcal{F}_c\{f'(x)\} = \omega \mathcal{F}_s\{f(x)\} - \sqrt{\frac{2}{\pi}} f(0)$$

```markdown
d) Show that $\mathcal{F}_c\{f'(x)\} = \omega \mathcal{F}_s\{f(x)\} - \sqrt{\frac{2}{\pi}} f(0)$.
```

---

##### 2024 Fall — United Technical College / UTC (Assessment) — Short (4 × 2.5)
c) Show that the Fourier cosine transform is linear: $\mathcal{F}_c\{a f(t) + b g(t)\} = a \mathcal{F}_c\{f(t)\} + b \mathcal{F}_c\{g(t)\}$.

```markdown
c) Show that the Fourier cosine transform is linear: $\mathcal{F}_c\{a f(t) + b g(t)\} = a \mathcal{F}_c\{f(t)\} + b \mathcal{F}_c\{g(t)\}$.
```
