# Chapter 2: Scan Conversion Algorithms - Past Questions

### Quick Navigation

[2(a)](#2a) | [2(b)](#2b) | [3(a)](#3a) | [3(b)](#3b) | [7](#7)

---

## 2(a)

##### 2025

```markdown
2. a) What are the limitations of DDA? Write the algorithm (any case)
   which overcomes these limitations. [7]
```

##### 2024-A

```markdown
2. a) Write the algorithm for both condition of Bresenham's line
   drawing algorithm and derive the initial decision parameter
   for less than 1. [8]
```

##### 2024-B

```markdown
2. a) What is the difference between DDA and BLA? Discuss
   "Midpoint Circle Drawing Algorithm" for 8 Octants. Derive an
   expression for decision parameter for conditions mentioned below. [7]
   i. Initial decision parameter Po.
   ii. Decision parameter, when Pk < 0.
   iii. Decision parameter, when Pk > 0.
```

##### 2024-C

```markdown
2. a) Mention the limitations of DDA algorithm. Derive the
   necessary expression of decision parameters to rasterize
   a line with m<1. [8]
```

##### 2023-B

```markdown
2. a) What is scan conversion? How can equations be derived for
   drawing lines with slope greater than one using Bresenham's
   line drawing algorithm? Explain. [8]
```

##### 2023-C

```markdown
2. a) OR Consider a line from (5, 5) to (13, 9). Use Bresenham's
   algorithm to rasterize the line. [7]
```

##### 2022

```markdown
2. b) Consider a line from (2,1) to (8,3) using DDA algorithm
   to rasterize a line. [8]
```

##### 2019-A

```markdown
2. a) Explain the Bresenham's Line drawing algorithm with
   suitable example. [8]
```

##### 2019-B

```markdown
2. b) Explain the working of DDA line drawing algorithm with
   suitable examples. Write its advantage and disadvantage. [7]
```

---

## 2(b)

##### 2025

```markdown
2. b) Derive the ellipse drawing algorithm for both region
   with initial decision parameter. [8]
```

##### 2024-A

```markdown
2. b) Why Circle and Ellipse uses different point symmetry?
   Digitize the circle with radius 10. [7]
```

##### 2024-C

```markdown
2. b) Digitize one octant of a circle by using midpoint circle
   generation algorithm center at origin and radius is 12. [7]
```

##### 2023-A

```markdown
2. b) Why Circle and Ellipse uses different point symmetry?
   Explain the mid-point circle generation algorithm by
   calculating its initial decision parameter. [8]
```

##### 2021

```markdown
2. b) Digitize the first octant of a circle having radius
   r=8 and centered at (3,4) [7]
```

##### 2019-A

```markdown
2. b) Derive mid-point Circle algorithm. [7]
```

##### 2019-B

```markdown
2. b) OR Explain Symmetrical property of circle. Write midpoint
   circle algorithm and apply that algorithm to find the pixel
   values of the circle whose radius r=10 and centre of
   the circle = (0, 0).
```

---

## 3(a)

##### 2024-B

```markdown
2. b) Explain the boundary fill algorithm in detail. How this
   approach differs from flood fill? [8]
```

##### 2023-B

```markdown
3. a) How symmetric property of circle can be implemented to
   digitize circle having equation (x-4)^2 + (y+5)^2 = 36.
   Illustrate with necessary calculations using mid-point
   circle drawing algorithm. [8]
```

##### 2022

```markdown
3. a) Explain the boundary fill algorithm in detail. How
   this approach differs from flood fill? [8]
```

##### 2019-B

```markdown
3. a) Define Decision Parameter in Bresenham's line drawing
   algorithm. Digitize a circle (x-2)^2 + (y-3)^2 = 25 using
   a midpoint circle drawing algorithm. [8]
```

---

## 3(b)

##### 2023-A

```markdown
3. b) How is Scan line algorithm different from Flood fill
   algorithm for filling polygons? Explain. [7]
```

##### 2023-C

```markdown
2. b) Explain flood fill algorithm using four connected method.
   Differentiate between boundary and flood fill algorithm. [8]
```

---

## 7

##### 2024-A

```markdown
7. c) Boundary vs. Flood Fill Algorithm
```
