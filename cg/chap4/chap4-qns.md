# Chapter 4: Graphics in Three Dimensions - Past Questions

### Quick Navigation
[4(a)](#4a) | [4(b)](#4b) | [5(a)](#5a) | [6(a)](#6a) | [7](#7)

---

## 4(a)

##### 2024-B
```markdown
4. a) Discuss difference between Spline approximation curve versus 
   a interpolation curve. Derive an expression for the Perspective 
   Projection with necessary diagram. [7]
```

##### 2024-C
```markdown
4. a) Derive composite matrix for rotation about an arbitrary axis 
   in 3D. [8]

OR

What are non-planar surface? How can a Bezier surface be generated 
from Bezier curve? [8]
```

##### 2023-B
```markdown
4. a) How are Two Dimensional Rotations different from Three 
   Dimensional Rotations? Explain with the matrix representations. [8]
```

##### 2023-C
```markdown
4. a) Differentiate between Parallel and perspective projection 
   with an example. [7]
```

##### 2019-A
```markdown
4. a) Explain Beizer curve and also specify the properties of 
   Beizer curve [8]
```

---

## 4(b)

##### 2025
```markdown
5. a) Differentiate between parallel and perspective projection 
   with real life example for each projection. [7]
```

##### 2024-A
```markdown
4. b) Differentiate between Parallel and perspective projection 
   with real life application example. [7]
```

##### 2023-B
```markdown
4. b) Derive the blending function for parametric cubic curve 
   using Hermite interpolation technique. [7]
```

##### 2021
```markdown
4. b) How you represent different objects in 3D: Differentiate 
   between parallel and perspective projection with example? [7]
```

##### 2019-A
```markdown
4. b) Explain and derive parallel projection transformation matrix. [7]
```

##### 2019-B
```markdown
4. b) Derive the composite matrix for reflection an object about 
   an arbitrary axis in 3D Space. [7]
```

---

## 5(a)

##### 2025
```markdown
4. a) Derive the equation for cubic Bezier curve and find the 
   coordinate at t=0.2 with respect to the control points 
   (1, 1), (4,6) (8,-3) and (12, 2). [8]
```

##### 2024-B
```markdown
5. a) Derive the equation for cubic Bezier curve and find the 
   coordinate at t=0.2 with respect to the control points 
   (1, 1), (4, 6) (8, -3) and (12, 2). [8]
```

##### 2023-A
```markdown
5. a) Define blobby objects with example. What is cubic Bezier curve? 
   Derive the equation of Cubic Bezier curve. [8]
```

##### 2023-C
```markdown
5. a) The 3D viewing process is more complex than 2D viewing process. 
   Why? How the mismatch between 3D and 2D is compensated. Explain 
   the steps with its appropriate figure. [7]

b) What is cubic Bezier curve? Derive the equation of cubic 
   Bezier curve. [8]
```

##### 2022
```markdown
5. a) How do you represent 3D objects by using Polygon Tables? 
   How is the consistency of geometric data table checked and 
   what are the rules for generating error free polygon tables? [7]
```

##### 2019-B
```markdown
5. a) Explain and derive transformation matrix of 3D rotation 
   about a line not parallel to any one axis. [8]
```

---

## 6(a)

##### 2023-A
```markdown
6. a) Explain the 3D viewing pipeline. Briefly explain the significance 
   of World coordinate system, Viewing coordinate system, Normalized 
   viewing coordinate system and Device coordinate system in the 
   viewing pipeline. [8]
```

##### 2022
```markdown
6. a) Derive the equation for cubic Bezier curve and find the 
   coordinate at t=0.2 with respect to the control points 
   (1, 1), (4, 6) (8, -3) and (12, 2). [8]
```

---

## 7

##### 2025
```markdown
7. b) Fractal Geometry method
```

##### 2024-A
```markdown
7. b) 3D Representations
```

##### 2024-C
```markdown
7. b) Perspective Projection
```

##### 2023-A
```markdown
7. b) Parallel vs. Perspective Projection
```
