# Chapter 3: Two Dimensional Geometric Transformations and Viewing - Past Questions

### Quick Navigation
[2(b)](#2b) | [3(a)](#3a) | [3(b)](#3b) | [4(a)](#4a) | [4(b)](#4b) | [7](#7)

---

## 2(b)

##### 2023-B
```markdown
2. b) What is composite transformation. Explain why do we need 
   homogeneous coordinate system for transformation of computation 
   in computer graphics. [7]
```

---

## 3(a)

##### 2025
```markdown
3. a) What will be the final coordinates of a triangle with vertices 
   A(5,8), B(15,8), C(10,16) after rotating it about any fixed point 
   (10,5) by 90-degree in clockwise direction. [8]
```

##### 2024-A
```markdown
3. a) What will be the final coordinates of a triangle with vertices 
   A(5,8), B(15,8), C(10,16) after rotating it about any fixed point 
   (10,5) and then shifting it down by two units? [8]
```

##### 2024-B
```markdown
3. a) Differentiate between Window and Viewport. Discuss the 
   2-Dimensional Viewing pipe line with different steps involved, 
   and further derivation needed for window to viewport coordinate 
   Transformation. [7]
```

##### 2024-C
```markdown
3. a) A graphics designer needs to animate the swing of pendulum in 
   center of display window with string attached at pivot point (350,300). 
   If pendulum is displaced by 45 degrees from its resting position. 
   Calculate the extreme points of bob's trajectory if string length 
   is of 10 units. [8]

OR

Reflect the triangle A(4,3), B(5,5) and C(4,6) about an axis defined 
by equation of line y=2-2x. [8]
```

##### 2023-A
```markdown
3. a) What is uniform scaling? Reflect an object (2, 3), (4, 3), (4, 5) 
   about line y = x + 1. [8]
```

##### 2023-B
```markdown
3. a) OR What is window and viewport? Magnify the triangle with 
   vertices A(0,0), B(1,1) and C(5,2) to thrice its size while 
   keeping C(5,2) fixed. [8]
```

##### 2023-C
```markdown
3. a) Find the final coordinates of a triangle with vertices A(5,8), 
   B(15,8), C(10,16) after reflecting it about line y = -2x - 2. [8]
```

---

## 3(b)

##### 2025
```markdown
3. b) Differentiate between window and viewport. Show the necessary 
   steps for mapping processes from world coordinate to device 
   coordinate along with mathematical derivation. [7]
```

##### 2024-A
```markdown
3. b) Define window and viewport. Derive a matrix for windows to 
   viewport transformation along with necessary steps. [7]
```

##### 2024-B
```markdown
3. b) A mirror is placed vertically such that it passes through 
   the points (5, 0) and (0, 5). Find the reflected view of triangle 
   ABC with coordinates A (5, 30), B (30, 50) and C (20, 60). [8]

OR

What is the significance of Clipping in Computer Graphics? Let ABCD 
be the rectangular window A(20,20), B(90,20), C(90,70) & D(20,70). 
Find the region code for the end points and use Cohen-Sutherland 
Line Clipping Algorithm to Clip the Line P1P2 with P1(10,30) 
and P2(80,90). [7]
```

##### 2024-C
```markdown
3. b) Why is it required to map an object description from a window 
   to a viewport? A triangle with vertices A(30,30) B(50,30) C(40,35) 
   is required to be mapped from a window with its lower left corner 
   at (20,20) and upper right corner at (60,40) to a viewport with 
   its lower left corner at (50,50) and upper right corner at (80,70). [7]
```

##### 2023-B
```markdown
3. b) Explain the Sutherland Hodgeman polygon clipping algorithm 
   considering the four different cases with example. [7]

OR

Let R be the rectangular window whose lower left hand corner is 
at L(-3, 1) and upper right-hand corner is at R (2, 6). Use 
Cohen-Sutherland algorithm to clip the line segments A (-4, 2) 
and B (-1, 7). [7]
```

##### 2023-C
```markdown
3. b) Window is defined as (20,20), (90,20), (90,70), (20,70). 
   Use Cohen Sutherland line clipping algorithm to clip the 
   line P(10,30) Q(80,90). [7]
```

##### 2022
```markdown
3. b) Prove that: [7]
   i. Two successive Translations are Additive
   ii. Two successive Scaling are Multiplicative
```

##### 2021
```markdown
3. a) Prove that successive translation and rotation is additive. [8]
```

##### 2021
```markdown
3. b) Explain the role of composite transformation in 2D/3D geometric 
   transformation. Explain viewing pipelining in 2D. [7]
```

##### 2019-A
```markdown
3. b) Explain 2D transformation using Homogeneous coordinator system. [7]
```

##### 2019-B
```markdown
3. b) Determine window to viewport transformation matrix for 
   window (5, 10) (15, 20) and for viewport (8, 12) (12, 18). 
   Note the coordinates values are for lower left and upper 
   right corner. [7]
```

---

## 4(a)

##### 2024-A
```markdown
4. a) Why is it required to map object description from the world 
   coordinates to the device coordinates? Explain the mapping 
   process with the necessary equations. [8]
```

##### 2023-A
```markdown
4. a) What do you understand by the term clipping? Explain the 
   Cohen-Sutherland line clipping algorithm with suitable 
   example. [8]
```

##### 2022
```markdown
4. a) Window port is given by (100,100,300,300) and viewport 
   is given by (50,50,150,150). Convert the window port 
   coordinate (200,200) to the viewport coordinate. [7]
```

##### 2021
```markdown
4. a) What is 3D transformation? Rotate the triangle A(0,0), 
   B(2,2), C(4,2) about the origin by an angle of 45°. [8]
```

##### 2019-A
```markdown
3. a) What is clipping? Explain Cohen Sutherland's line clipping 
   algorithm with suitable example. [8]
```

##### 2019-B
```markdown
4. a) Why do you need clipping? Explain the Cohen Sutherland 
   line clipping algorithm. [8]
```

---

## 4(b)

##### 2024-C
```markdown
4. b) Rotate the triangle with vertices A(3,3), B(5,3), C(4,5) 
   by 45° in anti clockwise direction while keeping B(5,3) fixed. [7]
```

##### 2022
```markdown
4. b) A mirror is placed vertically such that it passes through 
   the points (5, 0) and (0, 5). Find the reflected view of 
   triangle ABC with coordinates A (5, 30), B (30, 50) and C (20, 60). [8]
```

---

## 7

##### 2024-C
```markdown
7. a) 2D Viewing pipeline
7. c) Window to viewport transformation
```

##### 2023-B
```markdown
7. b) 2D viewing pipeline
```

##### 2021
```markdown
7. a) 2D viewing pipeline
```
